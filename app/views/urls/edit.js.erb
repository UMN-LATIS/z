$('table.data-table').DataTable().draw();
$(document).find("tr#url-<%= @url_identifier %>").replaceWith($("<%= escape_javascript(render(partial: 'urls/in_row_form', locals: { url: @url, url_identifier: @url_identifier })) %>"));
