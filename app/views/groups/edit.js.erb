$(document).find("tr#group-<%= @group_identifier %>").replaceWith($("<%= escape_javascript(render(partial: 'groups/in_row_form', locals: { group: @group, group_identifier: @group_identifier })) %>"));
