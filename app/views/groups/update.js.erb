<% if @group.errors.present? %>
	$(document).find("tr#group-<%= @group.id %>").replaceWith($("<%= escape_javascript(render(partial: 'groups/in_row_edit', locals: { group: @group })) %>"))
<% else %>
	<%= render partial: "show.js", locals: {group: @group, row_identifier: @group.id} %>
<% end %>
