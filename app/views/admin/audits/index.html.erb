<%- model_class = Audit -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>

<table id="audits-table" class="table audit-table table-striped table-bordered data-table" data-source="<%= datatable_admin_audits_path(format: :json) %>">
  <thead>
  <tr>
    <th></th>
    <th><%= t('helpers.table_headers.audit_item')  %></th>
    <th><%= t('helpers.table_headers.audit_event')  %></th>
    <th><%= t('helpers.table_headers.audit_whodunnit')  %></th>
    <th><%= t('helpers.table_headers.created_on') %></th>
  </tr>
  </thead>
  <tbody>

  </tbody>
</table>


<%= content_tag :div, class: "route-info", data: {new_transfer_request_path: new_admin_transfer_request_path} do %>
<% end %>


<%= render 'urls/modal' %>
