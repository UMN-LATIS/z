<nav class="navbar navbar-default" role="navigation">
   <div class="container-fluid">

       <ul class="nav navbar-nav navbar-left">
         <% if signed_in? %>

           <li class="<%= current_page?(urls_path) ? "selected" : "" %>">
             <%= link_to (urls_path) do  %>
               <span>
                 <i class="fa fa-link" aria-hidden="true"></i>
                 <span > URLs </span>
               </span>
             <% end %>
           </li>
           <li class="<%= current_page?(groups_path) ? "selected" : "" %>">
             <%= link_to (groups_path) do  %>
               <span>
                 <i class="fa fa-users" aria-hidden="true"></i>
                 <span > Groups </span>
               </span>
             <% end %>
           </li>

           <%if current_user.admin? %>
             <li class="<%= current_page?(admin_members_path) ? "selected" : "" %>">
               <a href="<%= admin_members_path %>">Manage Admins</a>
             </li>
           <%end%>

         <% end %>
       </ul>
       <div id="nav_context_switcher">
         <%= render 'layouts/nav/context_switcher' %>
       </div>
   </div>
 </nav>
